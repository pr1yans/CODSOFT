import random
import string
password = int(input("enter the length of your password ->  "))
characters = string.ascii_letters + string.digits + string.ascii_lowercase + string.ascii_uppercase + string.hexdigits + string.whitespace + string.punctuation 
key = "".join(random.choice(characters) for i in range(password))

print("Here is your Password ->", key)
